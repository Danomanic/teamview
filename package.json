{
  "name": "teamview",
  "version": "0.1.0",
  "description": "Static org-structure chart rendered from team.yaml files, hosted on GitLab Pages.",
  "type": "module",
  "private": true,
  "scripts": {
    "build:org": "node scripts/build-org.mjs",
    "predev": "npm run build:org",
    "dev": "vite",
    "prebuild": "npm run build:org",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "d3-org-chart": "3.1.1",
    "fuse.js": "^7.0.0"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "vite": "^5.4.0"
  }
}
